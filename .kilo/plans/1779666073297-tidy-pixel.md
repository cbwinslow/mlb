# Schema Refactor Implementation Plan

## Current State Analysis

### Completed Elements (Good)
- ✅ Deleted redundant files: `sql/040_raw/003_raw_statcast_migration_v2.sql`, `sql/050_staging/002_game_bridge.sql`, `sql/060_core/004_core_pitch_alter.sql`
- ✅ Core gameplay tables created with correct structure (`core.games`, `core.plate_appearances`, `core.pitches`)
- ✅ Serving view `core.v_unified_plate_appearances` implemented with `has_pitch_telemetry` flag
- ✅ Game identity bridge enhanced with comprehensive functionality (indexes, triggers, audit logs)
- ✅ Player identity validation functions added (013, 014)

### Issues Identified

#### 1. File Numbering Inconsistency
- Blueprint specifies `sql/050_staging/005_game_identity_bridge.sql`
- Current file is named `sql/050_staging/004_game_identity_bridge.sql`
- Need to rename to match blueprint numbering

#### 2. Schema Type Mismatches
- `core.games`, `core.plate_appearances`, `core.pitches` use UUID primary keys
- `core.player`, `core.team`, `core.venue` use BIGSERIAL primary keys
- `core_player_team_season` references `core.game` but should reference `core.games`
- Index file references old table names (`core.plate_appearance`, `core.pitch`, `core.game`)

#### 3. Ingestion Functions Not Updated
- `sql/080_functions/006_core_functions.sql` references `core.game` (should be `core.games`)
- Need to verify all functions handle new UUID-based primary keys

#### 4. Documentation Gaps
- AGENTS.md staging file map shows `004_game_identity_bridge.sql` but should show `005`
- OBJECTIVES.md section references need updating
- File comments in SQL files reference old table names

---

## Implementation Steps

### Step 1: Clean Up Staging Directory Structure
1. Rename `sql/050_staging/004_game_identity_bridge.sql` to `sql/050_staging/005_game_identity_bridge.sql`
2. Update references in AGENTS.md staging layer file map

### Step 2: Fix Schema Type Mismatches
1. Update `sql/060_core/003_core_relationships.sql`:
   - Change `core.game` references to `core.games` in `core.player_team_season`
2. Update `sql/090_constraints_indexes/006_core_indexes.sql`:
   - Rename `core_pa_game_idx` to reference `core.plate_appearances`
   - Rename `core_pitch_game_idx` to reference `core.pitches`
   - Add missing indexes on `core.plate_appearances(game_id, batter_id, pitcher_id)`
   - Add missing indexes on `core.pitches(plate_appearance_id)`

### Step 3: Update Core Functions
1. Update `sql/080_functions/006_core_functions.sql`:
   - Change `trg_core_game_updated_at` to reference `core.games` (not `core.game`)
2. Update `sql/080_functions/001_meta_functions.sql`:
   - Verify any game-related function references

### Step 4: Update Documentation
1. AGENTS.md:
   - Update staging layer file map to show `005_game_identity_bridge.sql`
   - Update core layer file map if needed
   - Update outstanding work section
2. OBJECTIVES.md:
   - Update section 8 "Schema Refactor" references if needed
3. sql/README.md:
   - Update file tree structure if present

---

## Detailed File Changes

### File: `sql/050_staging/004_game_identity_bridge.sql` → `sql/050_staging/005_game_identity_bridge.sql`
**Action**: Rename file via git mv
**Reason**: Blueprint specifies `005_game_identity_bridge.sql` for the canonical game identity bridge

### File: `sql/060_core/003_core_relationships.sql`
**Changes needed**:
- Line 15: `REFERENCES core.game(game_id)` → `REFERENCES core.games(game_id)`
- Line 19: `REFERENCES core.game(game_id)` → `REFERENCES core.games(game_id)`
- Add comment clarifying the relationship uses UUID game_id

### File: `sql/090_constraints_indexes/006_core_indexes.sql`
**Changes needed**:
- Line 39: `core.plate_appearance` → `core.plate_appearances` (table name)
- Line 51: `core.pitch` → `core.pitches` (table name)
- Line 19: Add comment - this indexes old `core.game` table, needs review
- Add new indexes per blueprint:
  ```sql
  CREATE INDEX IF NOT EXISTS core_plate_appearances_game_batter_pitcher_idx
      ON core.plate_appearances (game_id, batter_id, pitcher_id);
  CREATE INDEX IF NOT EXISTS core_pitches_plate_appearance_idx
      ON core.pitches (plate_appearance_id);
  ```

### File: `sql/080_functions/006_core_functions.sql`
**Changes needed**:
- Line 55: `trg_core_game_updated_at` references `core.game` - needs to be `core.games`

### File: `AGENTS.md`
**Changes needed**:
- Line 80: Update staging file map from `004_game_identity_bridge.sql` to `005_game_identity_bridge.sql`
- Verify core layer references are consistent

### File: `OBJECTIVES.md`
**Changes needed**:
- Section 8 "Schema Refactor" - verify all table names are current (`core.pitches` not `core.pitch`)

---

## Detailed Findings from File Reads

### Current File Analysis (2026-05-25)

#### File: `sql/050_staging/004_game_identity_bridge.sql`
- **Status**: Well-implemented with comprehensive functionality
- **Contents**: Adds `bbref_game_id`, `espn_game_id`, `odds_game_id` columns to `stg.game_identity`
- **Includes**: Unique indexes, updated_at trigger, resolution audit log, auto-resolution trigger function
- **Missing**: File should be renamed to `005_game_identity_bridge.sql` per blueprint

#### File: `sql/090_constraints_indexes/006_core_indexes.sql`
- **Issues Found**:
  - Line 38-39: `core_pa_game_idx` on `core.plate_appearance` (should be `core.plate_appearances`)
  - Line 41-45: Multiple indexes on `core.plate_appearance` (should be `core.plate_appearances`)
  - Line 50-51: `core_pitch_game_idx` on `core.pitch` (should be `core.pitches`)
  - Line 53-60: Multiple indexes on `core.pitch` (should be `core.pitches`)
  - Missing: `core_plate_appearances_game_batter_pitcher_idx`
  - Missing: `core_pitches_plate_appearance_idx`

#### File: `sql/080_functions/006_core_functions.sql`
- **Issue Found**: Lines 86-90 create `trg_core_game_updated_at` on `core.game` (should be `core.games`)
- **Note**: Core tables use UUID primary keys (`core.games.game_id` is UUID)

#### File: `sql/060_core/003_core_relationships.sql`
- **Issues Found**:
  - Line 14-15: `first_game_id` references `core.game(game_id)` (should be `core.games`)
  - Line 18-19: `last_game_id` references `core.game(game_id)` (should be `core.games`)
  - Note: `core.games.game_id` is UUID, not BIGINT

#### File: `AGENTS.md`
- **Issue Found**: Line 80 shows `004_game_identity_bridge.sql` (should be `005_game_identity_bridge.sql`)
- **Status**: Other references appear correct (`core.pitches`, `core.plate_appearances`, `core.games` on lines 90-92)

---

## Implementation Plan for 4 Specific Items

### Item 1: Clean Up sql/050_staging/ Directory

**Objective**: Rename `004_game_identity_bridge.sql` to `005_game_identity_bridge.sql` per blueprint

**Steps**:
1. Execute `git mv sql/050_staging/004_game_identity_bridge.sql sql/050_staging/005_game_identity_bridge.sql`
2. Update AGENTS.md staging layer file map (line ~80) to reference `005_game_identity_bridge.sql`
3. Update any internal file references if needed
4. Verify file is tracked properly by reading it

**Files affected**:
- `sql/050_staging/004_game_identity_bridge.sql` (to be renamed)
- `AGENTS.md` (update staging layer file map)

---

### Item 2: Add Core Gameplay Indexes

**Objective**: Add missing indexes on core gameplay tables

**Steps**:
1. Read `sql/090_constraints_indexes/006_core_indexes.sql` to understand current structure
2. Add missing indexes:
   - `core_plate_appearances_game_batter_pitcher_idx` on `core.plate_appearances(game_id, batter_id, pitcher_id)`
   - `core_pitches_plate_appearance_idx` on `core.pitches(plate_appearance_id)`
3. Fix any incorrect index names referencing old table names (`core.plate_appearance` → `core.plate_appearances`, `core.pitch` → `core.pitches`)
4. Add proper comments documenting each index

**Files affected**:
- `sql/090_constraints_indexes/006_core_indexes.sql`

---

### Item 3: Verify/Update Ingestion Functions in sql/080_functions/

**Objective**: Ensure all ingestion functions properly reference new schema with UUID primary keys

**Steps**:
1. Read `sql/080_functions/006_core_functions.sql` - check `trg_core_game_updated_at` references `core.games` not `core.game`
2. Read `sql/080_functions/010_ingestion_ops_functions.sql` - verify game-related functions handle `core.games`
3. Read `sql/080_functions/012_source_ingestion_functions.sql` - check for any game/player ID references
4. Verify `stg.update_game_identity` procedure exists in `sql/050_staging/005_game_identity_bridge.sql` (once renamed)
5. Verify `stg.fn_auto_resolve_statcast_game` trigger is properly attached and functional
6. Update any incorrect references found

**Files to verify/update**:
- `sql/080_functions/006_core_functions.sql`
- `sql/080_functions/010_ingestion_ops_functions.sql`
- `sql/080_functions/012_source_ingestion_functions.sql`

---

### Item 4: Update Documentation

**Objective**: Update all documentation to reflect current file state and table names

**Steps**:
1. Update `AGENTS.md`:
   - Staging layer file map: `004_game_identity_bridge.sql` → `005_game_identity_bridge.sql`
   - Core layer file map: verify `core.pitches`, `core.plate_appearances`, `core.games` are correctly referenced
   - Outstanding work section: update status checkboxes
   - Last updated date at top of file

2. Update `OBJECTIVES.md`:
   - Section references for `core.pitches` (not `core.pitch`)
   - Any game-related table name references

3. Update `sql/README.md`:
   - File tree structure if present
   - Any table name references

**Files affected**:
- `AGENTS.md`
- `OBJECTIVES.md`
- `sql/README.md`

---

## Implementation Status: PARTIALLY COMPLETED - REMAINING WORK

### Work Completed (Verification)
✅ Deleted redundant files: 
- `sql/040_raw/003_raw_statcast_migration_v2.sql`
- `sql/050_staging/002_game_bridge.sql` 
- `sql/060_core/004_core_pitch_alter.sql`

✅ Core tables created correctly:
- `core.games` (UUID PK)
- `core.plate_appearances` (UUID PK, FK to core.games)
- `core.pitches` (UUID PK, FK to core.plate_appearances)

✅ Serving view updated:
- `core.v_unified_plate_appearances` with `has_pitch_telemetry` flag

✅ Staging file structure corrected:
- Renamed `004_game_identity_bridge.sql` → `005_game_identity_bridge.sql`
- Fixed numbering to avoid conflicts (`006_source_conformance.sql`)
- Updated AGENTS.md and sql/README.md

✅ Core indexes fixed:
- Corrected table names: `core.plate_appearance` → `core.plate_appearances`, `core.pitch` → `core.pitches`
- Added missing indexes

## Remaining Work Items

### Item 1: Game Identity Bridge Implementation (Blueprint Compliance)
**Issue**: Current implementation extends `stg.game_identity` but blueprint requires simple mapping table
**File**: `sql/050_staging/005_game_identity_bridge.sql`
**Action**: Replace with blueprint-specified simple bridge table

### Item 2: Ingestion Function Updates
**Issue**: Missing logic to connect raw data to new canonical tables via bridge
**Files**: 
- `sql/080_functions/003_statcast_mlbapi_functions.sql`
- `sql/080_functions/005_staging_functions.sql`
**Action**: Add bridge resolution, core table inserts with UUID capture

### Item 3: Documentation Completeness
**Issue**: Missing documentation updates specified in blueprint
**Files**:
- `OBJECTIVES.md` (add Schema Refactor section)
- `ROADMAP.md` (add milestone)
- `architecture.md`, `data-dictionary.md` (update core table docs)
- All relevant docs (update references)
- `docs/audit_checklist.md` (create documentation audit)

## Implementation Steps

### Step 1: Replace Game Identity Bridge with Blueprint Specification
1. Replace contents of `sql/050_staging/005_game_identity_bridge.sql` with:
```sql
BEGIN;

CREATE TABLE IF NOT EXISTS staging.game_identity_bridge (
    canonical_game_id UUID NOT NULL DEFAULT gen_random_uuid(),
    source_system     VARCHAR(30) NOT NULL, -- 'retrosheet', 'mlb_api', 'statcast'
    source_game_key   VARCHAR(50) NOT NULL, -- e.g., 'BOS202604010' or '747124'
    season            INT NOT NULL,
    game_date         DATE NOT NULL,
    home_team_code    CHAR(3) NOT NULL,
    away_team_code    CHAR(3) NOT NULL,
    created_at        TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    CONSTRAINT pk_game_identity_bridge PRIMARY KEY (source_system, source_game_key)
);

CREATE INDEX IF NOT EXISTS idx_stg_game_bridge_canonical 
ON staging.game_identity_bridge(canonical_game_id);

COMMIT;
```
2. Verify `stg.game_identity` table remains unchanged (it should keep its existing columns including the game ID columns we added)

### Step 2: Update Ingestion Functions
#### For sql/080_functions/003_statcast_mlbapi_functions.sql:
1. Add logic to resolve game identity using the bridge/function
2. Insert into `core.plate_appearances` and capture UUID via RETURNING
3. Use that UUID when inserting into `core.pitches`
4. Ensure proper error handling

#### For sql/080_functions/005_staging_functions.sql:
1. Add similar logic for stadium/chadwick ingestion pipelines
2. Ensure consistency with statcast function approach

### Step 3: Complete Documentation Updates
1. Add “Schema Refactor” section to `OBJECTIVES.md` summarizing changes
2. Add milestone entry to `ROADMAP.md`: “Schema Refactor Completed – May 2026”
3. Update `architecture.md` to show new core table relationships
4. Update `data-dictionary.md` with column definitions for:
   - `core.games`
   - `core.plate_appearances` 
   - `core.pitches`
   - `staging.game_identity_bridge`
5. Update all relevant READMEs and docs markdown files to reflect new table names
6. Perform documentation audit:
   - Search for remaining old table name references (`core.pitch`, `core.game_bridge`, etc.)
   - Create `docs/audit_checklist.md` listing each doc scanned and status

### Step 4: Final Verification
1. Verify indexes in `sql/090_constraints_indexes/006_core_indexes.sql` include:
   - `core.plate_appearances(game_id, batter_id, pitcher_id)`
   - `core.pitches(plate_appearance_id)`
2. Ensure all SQL files follow `BEGIN; … COMMIT;` and use `IF NOT EXISTS`
3. Verify table/column comments are present for non-obvious fields
4. Confirm that `stg.game_identity` retains its extended columns (bbref_game_id, etc.) while the bridge remains simple

## Risk Assessment
- **Low Risk**: Documentation updates, simple table replacement
- **Medium Risk**: Ingestion function updates (require understanding of existing logic)
- **Mitigation**: Review existing function patterns before making changes, ensure backward compatibility where possible


### Summary of Changes Made

#### Item 1: Clean Up sql/050_staging/ Directory ✅
- Renamed `sql/050_staging/004_game_identity_bridge.sql` to `sql/050_staging/005_game_identity_bridge.sql`
- Renamed `sql/050_staging/005_source_conformance.sql` to `sql/050_staging/006_source_conformance.sql` (to avoid numbering conflict)
- Updated AGENTS.md staging layer file map
- Updated sql/README.md file tree

#### Item 2: Add Core Gameplay Indexes ✅
- Fixed table names in indexes: `core.plate_appearance` → `core.plate_appearances`, `core.pitch` → `core.pitches`
- Added `core_plate_appearances_game_batter_pitcher_idx` index
- Added `core_pitches_plate_appearance_idx` index
- Added `core_pitches_sequence_idx` index

#### Item 3: Verify/Update Ingestion Functions ✅
- Fixed `sql/080_functions/006_core_functions.sql`: Removed trigger for `core.games` (table doesn't have `updated_at` column)
- Verified `sql/080_functions/010_ingestion_ops_functions.sql` - no game table references
- Verified `sql/080_functions/012_source_ingestion_functions.sql` - no game/player ID issues

#### Item 4: Update Documentation ✅
- Updated AGENTS.md staging file map with correct file numbers
- Updated sql/README.md file tree
- Updated "Last updated" date in AGENTS.md

#### Additional Fixes ✅
- Fixed `sql/060_core/003_core_relationships.sql`: Changed `core.game` → `core.games` and fixed type from `BIGINT` to `UUID` for game_id references

---