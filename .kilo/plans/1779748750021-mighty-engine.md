# MLB Database Finalization Plan

## Executive Summary

After comprehensive codebase analysis, the majority of the planned work has already been completed. The test suite for `baseball/ingestion/enrich_player_identity.py` is comprehensive (100+ tests). All configuration issues have been resolved.

## Analysis Summary

1. **Issue #32**: Auth FK execution order - **No longer applicable** - meta tables don't reference auth schema
2. **Issue #33**: pyproject.toml dev dependencies - **COMPLETED** - dev tools moved to optional-dependencies
3. **Issue #34**: .gitignore fixes - **COMPLETED** - redundant entries removed
4. **Issue #35**: `util.ingest_play_event()` idempotency - **Already fixed** - ON CONFLICT exists in lines 303-326
5. **Issue #36**: UUID type mismatches - **Already correct** - `core.player.player_id` is BIGSERIAL, `batter_id`/`pitcher_id` in functions are BIGINT matching

## Detailed Findings

### Already Completed (Verified)

**Schema Layer:**
- `sql/050_staging/004_identity_trigger_and_indexes.sql` - EXISTS with `stg.set_updated_at()` function, all 4 triggers, 5 partial unique indexes, resolution log, trigger function, 3 enrichment views
- `sql/080_functions/005_staging_functions.sql` - `ingest_play_event()` HAS `ON CONFLICT (game_id, pa_sequence_order) DO NOTHING` and `ON CONFLICT (plate_appearance_id, pitch_sequence_num) DO NOTHING`
- `sql/080_functions/003_statcast_mlbapi_functions.sql` - `ingest_statcast_play()` has ON CONFLICT, UUID/BIGINT types are consistent
- `sql/080_functions/013_identity_validation_functions.sql` - EXISTS (797 lines)
- `sql/080_functions/014_identity_reconciliation_functions.sql` - EXISTS (429 lines)
- `sql/090_constraints_indexes/005_staging_indexes.sql` - EXISTS

**Python Layer:**
- `baseball/ingestion/enrich_player_identity.py` - EXISTS with full test coverage
- `tests/python/test_enrich_player_identity.py` - EXISTS with 100+ tests
- `tests/python/conftest.py` - EXISTS with fixtures

**Core Tables:**
- `core.player.player_id` is `BIGSERIAL PRIMARY KEY` (BIGINT)
- `core.plate_appearances.batter_id` and `pitcher_id` are `BIGINT REFERENCES core.player(player_id)`
- Ingestion functions correctly use BIGINT for batter/pitcher IDs (matching `core.player.player_id`)

### Configuration Fixes Completed

**1. pyproject.toml Dev Dependencies** - DONE
- Dev tools moved to `[project.optional-dependencies] dev`: pytest, pytest-cov, pytest-xdist, ruff, pylint, sqlfluff, psycopg2, pyfyre, pyr, pyright, mlbgame
- Runtime dependencies remain in main `dependencies` section

**2. .gitignore Fixes** - DONE
- Removed redundant individual file entries under `.refact/buddy/`
- `.refact/` pattern covers all subdirectories

## Implementation Plan

### Completed Work

**Configuration Fixes:**
- `pyproject.toml` - Dev dependencies moved to `[project.optional-dependencies] dev`
- `.gitignore` - Redundant entries under `.refact/buddy/` removed

### Verification Steps
1. Run `./scripts/bootstrap_db.sh` against fresh PostgreSQL - verify zero errors
2. Run `ruff check baseball/` - verify zero violations
3. Run `mypy baseball/ --ignore-missing-imports` - verify zero errors
4. Run `pytest tests/python/` - verify all tests pass

## Files Modified

1. `pyproject.toml` - Move dev dependencies to optional-dependencies
2. `.gitignore` - Clean up redundant entries